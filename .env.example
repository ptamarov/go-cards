# Database connection URL
# Format: host=<host> port=<port> dbname=<database> user=<username> password=<password>
# Example below uses PostgreSQL with default settings
DATABASE_URL=host=localhost port=5432 dbname=go_cards user=postgres password=
